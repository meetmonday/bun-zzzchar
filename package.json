{
  "name": "bun-zzzchar",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "bun --hot ./server",
    "build": "bun build ./server --compile --outfile ./dist/enka"
  },
  "type": "module",
  "devDependencies": {
    "bun-plugin-svelte": "^0.0.6"
  }
}
