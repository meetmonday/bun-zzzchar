{
  "name": "bun-zzzchar",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "bun --hot ./src/server",
    "build": "bun build ./src/server --compile --outfile ./dist/enka"
  },
  "type": "module",
  "devDependencies": {
    "bun-plugin-svelte": "^0.0.6"
  }
}
